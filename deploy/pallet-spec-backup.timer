# /etc/systemd/system/pallet-spec-backup.timer
[Unit]
Description=Pallet Spec nightly library snapshot

[Timer]
OnCalendar=*-*-* 02:00:00
# If the machine was off at two, run when it comes back.
Persistent=true

[Install]
WantedBy=timers.target
